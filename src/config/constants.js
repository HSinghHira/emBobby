/**
 * Shared constants used across commands, embeds, and services.
 * Keep this limited to genuinely cross-cutting values — anything
 * specific to a single module belongs in that module instead.
 */

export const COLORS = {
  primary: 0x5865f2, // Discord blurple
  success: 0x57f287,
  warning: 0xfee75c,
  error: 0xed4245,
};
